<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blood_donation_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hospital_blood_bank"></activity>
        <activity android:name=".hospital_signup_details" />
        <activity android:name=".hospital_profile" />
        <activity android:name=".hospital_new_donor" />
        <activity android:name=".hospital_recent_donor" />
        <activity android:name=".onlySignup" />
        <activity android:name=".onlyLogins" />
        <activity android:name=".donorDashboard" />
        <activity android:name=".hospitalDashboard" />
        <activity android:name=".hospitalSignup" />
        <activity android:name=".donorSignup" />
        <activity android:name=".hospitalLogin" />
        <activity android:name=".donorLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>